<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Stockist Scraper</title>
  <style>
    body { font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif; padding: 2rem; max-width: 720px; margin: auto; }
    .card { border: 1px solid #ddd; border-radius: 12px; padding: 1.5rem; }
    input[type="url"] { width: 100%; padding: 0.75rem; font-size: 1rem; border: 1px solid #ccc; border-radius: 8px; }
    button { padding: 0.75rem 1rem; font-size: 1rem; border-radius: 8px; border: 0; cursor: pointer; }
    .primary { background: black; color: white; }
    .muted { color: #666; font-size: 0.95rem; }
    .flash { background: #ffe4e6; padding: 0.75rem 1rem; border-radius: 8px; margin-bottom: 1rem; color: #9f1239; }
  </style>
</head>
<body>
  <h1>Extractor de revendeurs (Stockist)</h1>
  <p class="muted">Colle l’URL d’un store locator Stockist (ex: <code>https://pieceandlove.fr/pages/distributeurs</code>) et télécharge le CSV.</p>

  {% with messages = get_flashed_messages(with_categories=true) %}
    {% if messages %}
      {% for category, message in messages %}
        <div class="flash">{{ message }}</div>
      {% endfor %}
    {% endif %}
  {% endwith %}

  <div class="card">
    <form method="post" action="/scrape">
      <label for="url">URL du store locator</label><br>
      <input id="url" name="url" type="url" placeholder="https://…" required>
      <div style="margin-top: 1rem;">
        <button class="primary" type="submit">Extraire → CSV</button>
      </div>
    </form>
  </div>

  <p class="muted" style="margin-top: 1rem;">
    Fonctionne avec la plupart des intégrations <em>Stockist</em> (Shopify, WordPress).
  </p>
</body>
</html>
